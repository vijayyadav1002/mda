# Leave unset. The browser uses same-origin URLs via the Vite proxy (dev)
# or Caddy (production). Cookies require same-origin.
# Set VITE_API_URL only when the page origin is already the API origin.
# Pointing the SPA at http://localhost:4000 from :3000 is unsupported.
# VITE_API_URL=
